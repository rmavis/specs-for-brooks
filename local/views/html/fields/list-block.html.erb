<% if ((self.respond_to?(:_header)) && (self._header.get_out_val)) %>
    <h3 class="head-section"><%= self._header.get_out_val %></h2>
<% end %>
<ul>
    <% self._items.value.each do |item| %>
        <li><%= item %></li>
    <% end %>
</ul>
